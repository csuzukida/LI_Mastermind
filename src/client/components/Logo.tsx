import { Typography } from '@mui/joy';

const Logo = () => {
  return (
    <>
      <Typography level="h1" sx={{ color: 'text.secondary' }}>
        Masterm
        <Typography sx={{ color: '#0B6BCB' }}>in</Typography>
        <Typography sx={{ color: 'text.secondary' }}>d</Typography>
      </Typography>
    </>
  );
};

export default Logo;
